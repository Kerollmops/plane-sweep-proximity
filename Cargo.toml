[package]
name = "near-proximity"
version = "0.1.0"
authors = ["Clément Renault <clement@meilisearch.com>"]
edition = "2018"

[dependencies]
tinyvec = { version = "1.0.0", features = ["alloc"] }

[features]
default = []

# enable this feature to be able to `cargo bench`
nightly = []
